# Lõi pipeline: chuẩn hoá dữ liệu → backbone → classifier → đánh giá.
numpy>=1.24
scipy>=1.10
soundfile>=0.12
librosa>=0.10
PyYAML>=6.0
tqdm>=4.66

torch>=2.1
transformers>=4.40

scikit-learn>=1.3
matplotlib>=3.7

# Chỉ cần khi ingest từ HuggingFace Hub (`ingest --hf <repo_id>`).
datasets>=2.18
