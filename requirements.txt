# Lõi pipeline: chuẩn hoá dữ liệu → backbone → classifier → đánh giá.
numpy>=1.24
scipy>=1.10
soundfile>=0.12
librosa>=0.10
PyYAML>=6.0
tqdm>=4.66

torch>=2.1
# Trần <5 là vì engine kokoro-vietnamese dựa trên API transformers 4.x và tự từ chối
# chạy trên 5.x (Kaggle cài sẵn 5.x). Backbone WavLM/XLS-R/HuBERT/Whisper của dự án
# chạy được cả hai nhánh, nên hạ trần ở đây không mất gì.
transformers>=4.48,<5

scikit-learn>=1.3
matplotlib>=3.7

# Chỉ cần khi ingest từ HuggingFace Hub (`ingest --hf <repo_id>`).
datasets>=2.18
