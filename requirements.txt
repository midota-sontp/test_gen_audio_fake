# Lõi pipeline: chuẩn hoá dữ liệu → backbone → classifier → đánh giá.
numpy>=1.24
scipy>=1.10
soundfile>=0.12
librosa>=0.10
PyYAML>=6.0
tqdm>=4.66

torch>=2.1
# KHÔNG ghim trần ở đây: backbone WavLM/XLS-R/HuBERT/Whisper chạy tốt trên cả 4.x
# lẫn 5.x, trong khi hai engine sinh fake lại đòi hai nhánh ngược nhau
# (kokoro-vietnamese cần <5, omnivoice cần >=5.3). Ràng buộc đó thuộc về bước sinh
# fake — xem requirements-generate.txt.
transformers>=4.48

scikit-learn>=1.3
matplotlib>=3.7

# Chỉ cần khi ingest từ HuggingFace Hub (`ingest --hf <repo_id>`).
datasets>=2.18
