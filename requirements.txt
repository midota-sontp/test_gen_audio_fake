# Core DL (CPU wheels resolved via Dockerfile --extra-index-url for Docker;
# on Apple Silicon native, the default PyPI wheels already ship MPS support)
torch==2.2.2
torchaudio==2.2.2

# Audio processing
librosa==0.10.2.post1
soundfile==0.12.1

# Dataset
datasets==2.20.0
huggingface_hub==0.24.6
pyarrow==15.0.2

# ML / metrics / plots
scikit-learn==1.5.1
numpy==1.26.4
matplotlib==3.9.2
scipy==1.13.1

# Config / logging / progress
PyYAML==6.0.2
rich==13.7.1
tqdm==4.66.5

# Monitoring / experiment tracking / dashboard
streamlit==1.37.1
tensorboard==2.17.1
mlflow==2.15.1
psutil==6.0.0
